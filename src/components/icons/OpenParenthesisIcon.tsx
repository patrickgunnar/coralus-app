"use client";

const OpenParenthesisIcon = () => {
    return (
        <span className="text-[1rem] md:text-[1.5rem] font-medium font-sans"
        aria-hidden="true">
            (
        </span>
    );
};

export default OpenParenthesisIcon;
